{
  "buildCommand": "npm install --prefix abroadready",
  "installCommand": "npm install --prefix abroadready",
  "env": {
    "NODE_ENV": "production"
  },
  "functions": {
    "api/index.js": {
      "runtime": "nodejs18.x",
      "maxDuration": 30
    }
  }
}
